<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aaa.hgspringbootmybatis.dao.UserInfoDao">
    <insert id="saveUserInfo" parameterType="com.aaa.hgspringbootmybatis.entity.UserInfo">
       insert into userinfo values (#{userid},#{username},#{password})
    </insert>
    <update id="updateUserInfo" parameterType="com.aaa.hgspringbootmybatis.entity.UserInfo">
        update userinfo set password=#{password} where username=#{username}
    </update>
    <delete id="deleteUserInfoByname" parameterType="string">
        delete from userinfo where username=#{username}
    </delete>
    <select id="findUserByName" parameterType="string" resultType="com.aaa.hgspringbootmybatis.entity.UserInfo">
        select * from userinfo where username=#{username}
    </select>
    <select id="selectAllUser" resultType="com.aaa.hgspringbootmybatis.entity.UserInfo">
        select * from userinfo
    </select>

</mapper>